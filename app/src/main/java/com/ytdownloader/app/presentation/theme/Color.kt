package com.ytdownloader.app.presentation.theme

import androidx.compose.ui.graphics.Color

// ─── Ana renk paleti ──────────────────────────────────────────────────────────

val Primary = Color(0xFFFF0000)          // YouTube kırmızısı
val PrimaryDark = Color(0xFFCC0000)
val PrimaryLight = Color(0xFFFF4444)

val Secondary = Color(0xFF1A1A2E)        // Derin lacivert arka plan
val SecondaryVariant = Color(0xFF16213E)

val Accent = Color(0xFFE94560)           // Pembe aksan

// ─── Yüzey renkleri ──────────────────────────────────────────────────────────

val SurfaceDark = Color(0xFF0F3460)
val SurfaceCard = Color(0xFF1A1A2E)
val SurfaceElevated = Color(0xFF252547)

// ─── Metin renkleri ──────────────────────────────────────────────────────────

val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFE0E0E0)
val OnSurface = Color(0xFFFFFFFF)
val OnSurfaceVariant = Color(0xFFB0B0B0)

// ─── Durum renkleri ──────────────────────────────────────────────────────────

val Success = Color(0xFF4CAF50)
val Warning = Color(0xFFFF9800)
val Error = Color(0xFFF44336)
val Info = Color(0xFF2196F3)

// ─── Cam efekti (glassmorphism) ───────────────────────────────────────────────

val GlassBackground = Color(0x801A1A2E)
val GlassBorder = Color(0x40FFFFFF)
